#!/bin/bash

# NOTE: Make sure to run this from root of repo.

# First make sure we pull down our secrets
git submodule update

# There used to be more stuff here when ip_check.php existed but the python version handles everything
# from inside the script so... this file is kind of empty now. Keeping for potential future use.
